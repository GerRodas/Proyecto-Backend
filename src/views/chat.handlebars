<h1>Hola, bienvenidos al chat del ecommerce</h1>
<div>
    <input type="text" id="chatbox">
</div>
<div>
    <p id="messageLogs"></p>
</div>

<script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>


<!--div>
    
    <h1 class="title">Mensajes</h1>
    
    <table class="product-list">
        <tr>
            <th>Usuario</th>
            <th>Mensaje</th>
        </tr>
        {{#if chat.length}}
        {{#each chat}}
        <tr>
            <td>{{this.user}}</td>
            <td>{{this.message}}</td>
        </tr>
        {{/each}}
        {{/if}}
    </table>

</div>

<style>
    table,
    th,
    td {
        border: 1px solid;
    }
    .title {
        text-align: center;
    }
    .product-list {
        width: 100%;
    }
</style-->

